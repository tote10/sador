APP_NAME=Laravel
APP_ENV=local
APP_KEY=
APP_DEBUG=true
APP_TIMEZONE=UTC
APP_URL=http://localhost

APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US

APP_MAINTENANCE_DRIVER=file
# APP_MAINTENANCE_STORE=database

PHP_CLI_SERVER_WORKERS=4

BCRYPT_ROUNDS=12

LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

DB_CONNECTION=sqlite
# DB_HOST=127.0.0.1
# DB_PORT=3306
# DB_DATABASE=laravel
# DB_USERNAME=root
# DB_PASSWORD=

SESSION_DRIVER=database
SESSION_LIFETIME=120
# Log out when the browser closes; "Remember me" then keeps trusted users signed in.
SESSION_EXPIRE_ON_CLOSE=true
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null

BROADCAST_CONNECTION=log
FILESYSTEM_DISK=local
QUEUE_CONNECTION=database

CACHE_STORE=database
CACHE_PREFIX=

MEMCACHED_HOST=127.0.0.1

REDIS_CLIENT=phpredis
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

# ----------------------------------------------------------------------------
# EMAIL SENDING
# "log" writes emails to storage/logs/laravel.log instead of sending them — good
# for testing. To send REAL emails, set MAIL_MAILER=smtp and fill in the SMTP
# details below from your email provider (e.g. Gmail, your hosting, Brevo, Resend).
# Example (Gmail): HOST=smtp.gmail.com  PORT=587  SCHEME=tls  USERNAME=you@gmail.com
#   PASSWORD=<a Google "App Password", not your normal password>
# Always set MAIL_FROM_ADDRESS to a real address on your domain.
# ----------------------------------------------------------------------------
MAIL_MAILER=log
MAIL_SCHEME=null
MAIL_HOST=127.0.0.1
MAIL_PORT=2525
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_FROM_ADDRESS="hello@example.com"
MAIL_FROM_NAME="${APP_NAME}"

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

VITE_APP_NAME="${APP_NAME}"

# SEO / Analytics (leave empty to disable — no tracking or verification markup renders)
GOOGLE_ANALYTICS_ID=
GOOGLE_SITE_VERIFICATION=

# Admin account created by the database seeder.
# Leave ADMIN_EMAIL / ADMIN_PASSWORD blank and the seeder skips the admin
# (so no weak default password ever ships to production).
ADMIN_NAME="Sador Admin"
ADMIN_EMAIL=
ADMIN_PASSWORD=
